{
  "name": "@kb-labs/tox-bench",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Benchmarks and fixtures for KB Labs TOX",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsup",
    "dev": "tsup --watch",
    "type-check": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run -c ../../vitest.config.ts",
    "test:watch": "vitest -c ../../vitest.config.ts"
  },
  "dependencies": {
    "@kb-labs/tox-core": "link:../tox-core",
    "@kb-labs/tox-codec-json": "link:../tox-codec-json"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "vitest": "^3",
    "@kb-labs/devkit": "github:KirillBaranov/kb-labs-devkit#main",
    "tsup": "^8",
    "@types/node": "^24.7.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}

